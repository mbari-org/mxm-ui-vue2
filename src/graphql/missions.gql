query missionsForProvider($providerId: String!) {
  missionsForProvider(providerId: $providerId) {
    providerId
    provider {
      descriptionFormat
    }
    missionTplId
    missionId
    missionStatus
    assetId
    description
    startDate
    endDate
  }
}
